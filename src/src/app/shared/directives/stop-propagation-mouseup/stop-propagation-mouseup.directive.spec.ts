/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StopPropagationMouseupDirective } from './stop-propagation-mouseup.directive';

describe('Directive: StopPropagationMouseup', () => {
  it('should create an instance', () => {
    const directive = new StopPropagationMouseupDirective();
    expect(directive).toBeTruthy();
  });
});
