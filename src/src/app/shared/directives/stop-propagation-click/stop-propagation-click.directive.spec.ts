/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StopPropagationClickDirective } from './stop-propagation-click.directive';

describe('Directive: StopPropagationClick', () => {
  it('should create an instance', () => {
    const directive = new StopPropagationClickDirective();
    expect(directive).toBeTruthy();
  });
});
