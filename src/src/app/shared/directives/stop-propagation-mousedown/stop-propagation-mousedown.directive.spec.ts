/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StopPropagationMousedownDirective } from './stop-propagation-mousedown.directive';

describe('Directive: StopPropagationMousedown', () => {
  it('should create an instance', () => {
    const directive = new StopPropagationMousedownDirective();
    expect(directive).toBeTruthy();
  });
});
